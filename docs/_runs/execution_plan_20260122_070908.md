# No-Manual Execution Plan (Security Hardening)

## Branch / PR Strategy
- Branch: `fix/security-hardening-20260122_065037`.
- Single PR; small, focused commits.

## Remaining Tasks (if enforcing zero HIGH in backend)
1) Replace or isolate `xlsx` usage (legacy Express/scripts) with a maintained library, or remove legacy scripts entirely.
2) Remove or upgrade dependency chain that introduces vulnerable `tar`/`node-gyp` packages (likely via legacy tooling).
3) Re-run npm audit for CRM/backend until HIGH/CRITICAL = 0, or document and accept exceptions explicitly.

## Acceptance Criteria
- No committed secrets (`gitleaks` clean).
- `/api/dev/token` disabled outside dev, requires `DEV_TOKEN_PASSWORD` when enabled.
- Rate limiting enforced for sensitive endpoints and globally.
- npm audits: no HIGH/CRITICAL or documented exceptions for CRM/backend legacy dependencies.
- CI enforces gitleaks + Python security tooling.

## Automated Checks
- Frontend: `cd CRM/frontend && npm run build`
- PWA: `cd ALQASEER-PWA && npm run build`
- Backend (CI): ruff, bandit, pip-audit, semgrep
- Security: `gitleaks detect --source . --config .gitleaks.toml --redact --no-git`
