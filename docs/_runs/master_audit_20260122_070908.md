# Master Audit Report (dopamine-crm-suite-playground)

## Executive Summary
- P0: No committed secrets detected after cleanup; gitleaks scan is clean.
- P1: /api/dev/token now disabled by default and requires dev-only env + password.
- P1: npm audit clean for CRM/frontend, ALQASEER-PWA, AI-Orchestrator; CRM/backend still has 6 HIGH vulnerabilities (tar/xlsx chain).
- P2: Rate limiting middleware enforced (sensitive endpoints first, then global).
- P2: Python security tooling added to CI via requirements-dev + workflow.

## Evidence Index
- Gitleaks report: `docs/_runs/gitleaks_20260122_070908.json` (no leaks)
- npm audits:
  - `docs/_runs/npm_audit_CRM_frontend_20260122_070908.json`
  - `docs/_runs/npm_audit_ALQASEER-PWA_20260122_070908.json`
  - `docs/_runs/npm_audit_CRM_backend_20260122_070908.json`
  - `docs/_runs/npm_audit_AI-Orchestrator_20260122_070908.json`
- Secret hygiene config:
  - `.gitignore:17-18` (.env.local block)
  - `.gitleaks.toml:4-11` (env secret rule + allowlist for examples)
  - `.github/workflows/security-gitleaks.yml:18-21`
- Dev token hardening:
  - `CRM/backend/api/dev.py:23-62`
  - `CRM/backend/tests/test_dev_token.py:7-34`
  - `CRM/backend/.env.example:13-14`
  - `CRM/backend/README.md:81`
- Rate limiting enforcement:
  - `CRM/backend/middleware/rate_limit.py:68-97`
  - `CRM/backend/main.py:95`
  - `CRM/backend/tests/test_rate_limit.py:4-22`
- Python toolchain in CI:
  - `CRM/backend/requirements-dev.txt:1-4`
  - `.github/workflows/security-python-audit.yml:24-36`

## Security Findings

### P0 — Secrets cleanup
- Evidence:
  - `.gitignore:17-18` blocks `.env.local` and `.env.*.local`.
  - `.gitleaks.toml:8-11` ensures `.env` files are scanned while example files are allowlisted.
  - `docs/_runs/gitleaks_20260122_070908.json` shows no leaks.
- Status: addressed.

### P1 — Dev token endpoint hardening
- Evidence:
  - `CRM/backend/api/dev.py:23-62` requires `APP_ENV=development`, `ALLOW_DEV_TOKEN=1`, and `DEV_TOKEN_PASSWORD`.
  - `CRM/backend/tests/test_dev_token.py:7-34` verifies non-dev denial + password requirement.
- Status: addressed.

### P1 — Dependency vulnerabilities (npm audit)
- Evidence:
  - `docs/_runs/npm_audit_CRM_frontend_20260122_070908.json` (0 vulnerabilities).
  - `docs/_runs/npm_audit_ALQASEER-PWA_20260122_070908.json` (0 vulnerabilities).
  - `docs/_runs/npm_audit_AI-Orchestrator_20260122_070908.json` (0 vulnerabilities).
  - `docs/_runs/npm_audit_CRM_backend_20260122_070908.json` (6 HIGH vulnerabilities; packages include tar/xlsx and dependency chain via node-gyp).
- Status: partial; backend HIGH vulnerabilities remain (documented exception needed if kept).

### P2 — Rate limiting enforced
- Evidence:
  - `CRM/backend/middleware/rate_limit.py:82-97` applies sensitive then global limits.
  - `CRM/backend/tests/test_rate_limit.py:7-20` confirms 429s on repeated calls.
- Status: addressed.

## Build/Config Findings
- Backend dev token now requires explicit envs; documented in `CRM/backend/README.md:81` and `CRM/backend/.env.example:13-14`.
- Python security tools pinned in `CRM/backend/requirements-dev.txt` and executed in CI.

## PWA/GPS/Offline Findings
- Existing offline queue, GPS sampling, and service worker paths remain unchanged (see prior audit).

## Conflicts vs Other Reports
- Workik claim (SQLite vs Postgres, missing RBAC): evidence still shows SQLite default + Postgres support; RBAC enforcement present (see previous audit lines in `CRM/backend/README.md` and `CRM/backend/core/security.py`).
- Gemini claim (fractured PWA/app refs): duplicated backend subtree still present under `ALQASEER-PWA/CRM/backend`.
- Claude reports: no repo references found (see prior evidence in `docs/_runs/search_workik_gemini_claude_20260122_031606.txt`).

## Tooling Constraints
- Local Python runtime not used; CI handles Python security tooling.
