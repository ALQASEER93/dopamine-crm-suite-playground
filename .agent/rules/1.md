---
trigger: always_on
---

Output only under docs/_runs/run_<timestamp>/.
No writes to repo root or any other path.
On failure: write docs/_runs/run_<timestamp>/artifacts/PHASE_FAILURE_ANALYSIS.md, apply the smallest fix, and re-run only the failing step.
